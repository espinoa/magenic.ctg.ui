import { CapslockMonitorDirective } from './capslock-monitor.directive';

describe('CapslockMonitorDirective', () => {
  it('should create an instance', () => {
    const directive = new CapslockMonitorDirective();
    expect(directive).toBeTruthy();
  });
});
